# GP Alias Reproducibility
This document tests the gp alias by being created and staged.

## Creation Time
$(date)

## Expected Flow
1. This file is created
2. `git add .` stages it
3. `git commit` creates commit
4. `git push` sends to remote

## Verification
File should appear in git status as "new file"
